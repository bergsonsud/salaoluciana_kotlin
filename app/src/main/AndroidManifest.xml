<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salaoluciana">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CustomerListActivity">

        </activity>
        


        <activity android:name=".activities.FirebaseUIAuth">

        </activity>


        <activity android:name=".activities.AddCustomerActivity"/>

        <activity android:name=".activities.OrderListActivity">

        </activity>

        <activity android:name=".activities.ProductsListActivity">

        </activity>

        <activity android:name=".activities.AddProductActivity" />

        <activity android:name=".activities.Spinner"/>


        <activity android:name=".activities.HomeActivity">
        </activity>

        <activity android:name=".activities.AddOrderActivity"/>
        <activity android:name=".activities.AccountUserActivity"/>


        <activity android:name=".login.HomeLogin"/>
        <activity android:name=".login.LoginEmailActivity"/>

        <activity android:name=".activities.MainActivity"/>

        <activity android:name=".activities.TestActivity"/>

        <activity android:name=".activities.MainBottomSheetActivity"/>

        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>